# ADW project configuration. Commands must cite an observable project source.
schema: 5

git:
  default_branch: main

documentation:
  mode: branch
  branch: docs
  worktree: worktrees/docs
  sync_marker: SYNC.yaml
  delivery: direct-push

execution:
  isolation: managed-devcontainer
  enforcement: required
  permissions:
    profile: managed-development

components:
  app:
    path: .

validation:
  default: []

# Optional. Omit this block for lightweight projects.
# integrations:
#   work_tracker:
#     provider: azure-devops
#     requirement: optional
#     transport: auto
#     access: read-write
#     settings:
#       organization: "<organization>"
#       project: "<project>"

# Optional project workflow policy. A required binding is resolved during
# planning; configuration never authorizes the external mutation itself.
# workflows:
#   work_tracker:
#     binding: required
#     ensure: create-or-link
#     stage: plan
#     cardinality: one-per-change
#     profile: adw/work-items/feature-story.yaml
#     # Required only for one-parent-plus-plan-tasks:
#     child_profile: adw/work-items/implementation-task.yaml
