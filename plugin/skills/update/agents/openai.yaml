interface:
  display_name: "ADW Update"
  short_description: "Check current project compatibility"
  default_prompt: "Use $update to check whether this project uses the exact ADW schema supported by the installed plugin."
